import React from 'react';
import styled from 'styled-components';

const AddSchedule = () => {
  return <Container>AddSchedule</Container>;
};

const Container = styled.div``;

export default AddSchedule;
